import { StyleSheet } from 'react-native';

export const squareStyles = StyleSheet.create({
  container: {
    borderStyle: 'solid',
    borderWidth: 2,
    fontSize: 20,
    marginRight: -1,
    marginTop: -1,
    minWidth: 60,
    minHeight: 60,
    justifyContent: 'center',
  },
});
